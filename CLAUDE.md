# Overview

The `claude-tools` is a collection of tooling to enhance Claude Code.

At present the only tool is `tool-monitor`.

# Git and version control

The git repository is at the top-level, but we frequently work on a specific tool.
